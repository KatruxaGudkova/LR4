# 🌐 Обход графа в глубину (DFS) и поиск длины пути

Этот проект реализует **алгоритм обхода графа в глубину (Depth-First Search, DFS)** и функцию для **поиска длины пути между двумя вершинами** с обработкой ошибок.

---

## 🚀 Возможности

- Рекурсивная реализация **DFS** с выводом посещённых вершин  
- Поиск длины пути между двумя вершинами в **неориентированном графе**  
- Проверка корректности входных данных (наличие вершин в графе)  
- Обработка ошибок при вводе некорректных вершин  
- Пример работы с консольным выводом  

---

## 🧩 Используемые технологии

- **JavaScript (ES6+)** — язык реализации  
- **Node.js** (или браузерная консоль) — для запуска скрипта  

---

## ⚙️ Структура проекта
```
📁 project-root/
│
└── dfs.js # Основная логика обхода и поиска пути
```

---

## 🧠 Описание функций

| Функция | Назначение |
|----------|-------------|
| `dfs(graph, start, visited)` | Рекурсивно обходит граф в глубину, начиная с вершины `start` |
| `dfsPathLength(graph, start, end)` | Ищет длину кратчайшего пути между вершинами `start` и `end` с проверкой корректности входных данных |

---

## 🧠 Как работает алгоритм
🔹 1. Обход графа (DFS)
Алгоритм рекурсивно посещает вершины, начиная с заданной, двигаясь в глубину, пока есть непосещённые соседи.
Псевдокод:
DFS(v):
  отметить v как посещённую
  для каждого соседа u вершины v:
    если u не посещён:
      DFS(u)

🔹 2. Поиск длины пути

Алгоритм использует стек (итеративный DFS) для подсчёта длины пути между двумя вершинами.
Если одна из вершин отсутствует в графе — выбрасывается ошибка.

▶️ Запуск проекта

1. Установи Node.js
2. Склонируй репозиторий или скачай файл
3. Запусти скрипт:
   
node dfs.js

## ⚠️ Обработка ошибок
Функция dfsPathLength() выбрасывает ошибку, если:
одна или обе вершины отсутствуют в графе 
или данные имеют некорректный формат.

Пример:
Ошибка: Одна или обе вершины (2, 999) отсутствуют в графе или некорректны
